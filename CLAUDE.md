# 프로젝트 통합 지침서 (Project Guidelines)

이 파일은 Claude, Antigravity 등 프로젝트에 참여하는 모든 AI 어시스턴트가 공통으로 준수해야 하는 중앙 지침서이다.

## 1. 기본 작업 규칙
- 모든 자체 에셋과 스크립트는 `Assets/_Project/` 폴더 내부에서만 작업한다.
- `Assets/ThirdParty/` 및 `Assets/Test/` 폴더는 Git에 올라가지 않으므로, 공유가 필요한 작업물은 이곳에 두지 않는다.

## 2. 코딩 컨벤션
- 프로젝트 루트에 있는 `Unity6_CSharp_Convention.md`의 C# 코딩 규칙을 참고하여 작성한다.

## 3. AI 코딩 행동 원칙 (Karpathy Guidelines)
AI 에이전트는 코드 작성 및 수정 시 다음 4가지 원칙을 철저히 엄수한다.

1. **코딩 전 생각하기 (Think Before Coding)**
   - 요구사항이나 유니티 API에 대해 임의로 추측하여 코딩하지 않는다.
   - 모호한 부분이나 확신이 서지 않는 구조가 있다면 반드시 작업을 멈추고 사용자에게 질문하여 확인을 받는다.
2. **단순함 최우선 (Simplicity First)**
   - 문제를 해결할 수 있는 최소한의 코드만 작성한다.
   - 불필요한 기능 추가, 과도한 추상화 등 오버엔지니어링을 철저히 배제한다.
3. **외과 수술적 변경 (Surgical Changes)**
   - 목표 달성에 필수적인 파일과 코드 라인만 최소한으로 수정한다.
   - 멀쩡히 작동하는 주변 코드나 주석을 임의로 리팩토링(개선)하지 않는다.
   - 자신이 수정한 코드로 인해 방치된 찌꺼기 코드만 정리하며, 기존의 데드 코드는 지시가 없는 한 삭제하지 않는다.
4. **목표 주도적 실행 (Goal-Driven Execution)**
   - 작업을 수행할 때 명확한 성공 및 검증 기준(예: 컴파일 에러 없음, 프리팹 적용 완료 등)을 설정하고 이를 달성할 때까지 스스로 검증한다.

## 4. 에이전트 작업 기억 시스템 (Memory Protocol)
작업의 연속성을 잃지 않고 엉뚱한 방향으로 코딩하는 것을 방지하기 위해 기억(Memory) 시스템을 운영한다.

- AI 에이전트는 새로운 세션을 시작할 때마다 반드시 루트 디렉토리에 있는 `MEMORY.md` 파일을 먼저 읽고 현재 프로젝트의 진행 맥락을 파악한다.
- 대화(작업)를 종료하기 전, AI 에이전트는 자발적으로 `MEMORY.md` 파일을 업데이트하여 다음 사항을 최신화한다:
  1. **최근 완료 작업 및 주요 결정 사항**: 방금 수정한 핵심 내용과 채택된 아키텍처 규칙
  2. **현재 상태 및 이슈**: 미해결 버그나 알려진 문제점
  3. **다음 할 일 (Next Steps)**: 다음 작업자가 이어서 진행해야 할 목표
