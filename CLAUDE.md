# 프로젝트 통합 지침서 (Project Guidelines)

이 파일은 Claude, Antigravity 등 프로젝트에 참여하는 모든 AI 어시스턴트가 공통으로 준수해야 하는 중앙 지침서이다.

## 1. 기본 작업 규칙
- 모든 자체 에셋과 스크립트는 `Assets/_Project/` 폴더 내부에서만 작업한다.
- `Assets/ThirdParty/` 및 `Assets/Test/` 폴더는 Git에 올라가지 않으므로, 공유가 필요한 작업물은 이곳에 두지 않는다.
- 새로 작성하는 관리 문서(컨벤션, 가이드, 작업 기록 등)는 기본적으로 `docs/` 폴더 안에 저장한다.
  - 예외: `CLAUDE.md`, `AGENTS.md`, `README.md`는 각 도구(Claude Code, Antigravity 등)와 GitHub 관례상 루트에 있어야 하므로 그대로 둔다.
  - 예외: `.github/`(Issue/PR 템플릿), `.claude/skills/`, `.agent/skills/`처럼 특정 툴이 정해진 경로에서만 인식하는 파일은 해당 경로를 따른다.

## 2. 코딩 컨벤션
- AI 에이전트는 코드를 작성하거나 수정하기 전, 세션 중 아직 읽지 않았다면 반드시 `docs/coding-convention.md`를 먼저 읽고 그 규칙(명명 규칙, 서식, 네임스페이스, 클래스 선언 순서 등)을 따른다.

## 3. AI 코딩 행동 원칙 (Karpathy Guidelines)
AI 에이전트는 코드 작성 및 수정 시 다음 4가지 원칙을 철저히 엄수한다.

1. **코딩 전 생각하기 (Think Before Coding)**
   - 요구사항이나 유니티 API에 대해 임의로 추측하여 코딩하지 않는다.
   - 모호한 부분이나 확신이 서지 않는 구조가 있다면 반드시 작업을 멈추고 사용자에게 질문하여 확인을 받는다.
2. **단순함 최우선 (Simplicity First)**
   - 문제를 해결할 수 있는 최소한의 코드만 작성한다.
   - 불필요한 기능 추가, 과도한 추상화 등 오버엔지니어링을 철저히 배제한다.
3. **외과 수술적 변경 (Surgical Changes)**
   - 목표 달성에 필수적인 파일과 코드 라인만 최소한으로 수정한다.
   - 멀쩡히 작동하는 주변 코드나 주석을 임의로 리팩토링(개선)하지 않는다.
   - 자신이 수정한 코드로 인해 방치된 찌꺼기 코드만 정리하며, 기존의 데드 코드는 지시가 없는 한 삭제하지 않는다.
4. **목표 주도적 실행 (Goal-Driven Execution)**
   - 작업을 수행할 때 명확한 성공 및 검증 기준(예: 컴파일 에러 없음, 프리팹 적용 완료 등)을 설정하고 이를 달성할 때까지 스스로 검증한다.

## 4. 브랜치 & 커밋 컨벤션
- 이 프로젝트는 Git-flow 전략을 따른다. 브랜치 규칙(`feature/{이름}`, `release/{버전}`, `hotfix/{이름}`)과 커밋 메시지 태그 규칙은 `docs/commit-convention.md`를 따른다.
- AI 에이전트가 커밋 메시지를 작성하거나 브랜치를 생성할 때는 이 문서를 먼저 확인한다.

## 5. AI 지시사항 (응답 방식)
- AI 에이전트가 사용자에게 답변할 때는 `docs/ai-instructions.md`에 정의된 방식(결론 먼저, 근거는 그다음)을 따른다.

## 6. Intent 관리 시스템
사소한 수정 이상의 작업(새 기능, 구조 변경 등)을 시작하기 전에는 "왜, 무엇을 위해" 하는지를 `docs/intent/` 폴더에 문서로 먼저 정리한다. 상세 규칙과 템플릿은 `docs/intent/README.md`를 따른다.

- 작업 시작 전 `docs/intent/`(미해결)와 `docs/intent/clear/`(해결됨)에 관련 문서가 있는지 먼저 확인한다.
- 없으면 `docs/intent/_template.md`를 복사해 새 intent 문서를 작성한다 (문제, 기대 결과, 영향 범위, 제약, 열린 질문).
- 열린 질문이 남아 있으면 임의로 답을 추측하지 말고 사용자에게 확인한다 (3장의 "코딩 전 생각하기" 원칙과 연동).
- 작업이 끝나 문제가 해결되면 문서 상태를 `resolved`로 갱신하고 `docs/intent/clear/`로 이동한다.

## 7. 에이전트 작업 기억 시스템 (Handover Protocol)
작업의 연속성을 잃지 않고 엉뚱한 방향으로 코딩하는 것을 방지하기 위해, 참여자(사람/AI)별 인수인계 기록 시스템을 `docs/HANDOVER/`에 운영한다. 상세 규칙은 `docs/HANDOVER/README.md`를 따른다.

- AI 에이전트는 새로운 세션을 시작할 때마다 자신이 작업 중인 참여자 이름의 `docs/HANDOVER/{이름}/HANDOFF_POINTER.md`를 먼저 읽고, 최신 항목부터 진행 맥락을 파악한다. 해당 폴더가 없으면 `docs/HANDOVER/_template/`을 복사해 새로 만든다.
- 대화(작업)를 종료하기 전, AI 에이전트는 자발적으로 오늘 날짜 폴더(`docs/HANDOVER/{이름}/{YYYY-MM-DD}/`)에 아래 항목을 담은 작업 기록 문서를 새로 작성한다:
  1. **작업 요약 및 방법**: 무엇을, 어떻게 했는지
  2. **결정 사항**: 채택된 아키텍처 규칙
  3. **현재 상태 및 이슈**: 미해결 버그나 알려진 문제점
  4. **다음 할 일 (Next Steps)**: 다음 작업자가 이어서 진행해야 할 목표
- 그 후 `HANDOFF_POINTER.md` 맨 위에 날짜 + 한 줄 요약 + 문서 링크 형식으로 항목을 추가한다 (기존 항목은 지우지 않는다).
